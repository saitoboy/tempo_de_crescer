services:
  db:
    image: postgres:17-alpine
    container_name: tempo_de_crescer_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: tempo_de_crescer
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d tempo_de_crescer"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
